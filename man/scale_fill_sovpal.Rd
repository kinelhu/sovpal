% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2-scales.R
\name{scale_fill_sovpal}
\alias{scale_fill_sovpal}
\title{ggplot2 fill scale for sovpal palettes}
\usage{
scale_fill_sovpal(palette = "gost14202", discrete = TRUE, direction = 1, ...)
}
\arguments{
\item{palette}{Character. Palette name. Default \code{"gost14202"}.}

\item{discrete}{Logical. If \code{TRUE} (default), uses a discrete scale. If
\code{FALSE}, uses a continuous gradient via \code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradientn()}}.}

\item{direction}{Integer. \code{1} (default, normal) or \code{-1} (reversed).}

\item{...}{Additional arguments passed to the underlying ggplot2 scale.}
}
\value{
A ggplot2 scale object.
}
\description{
A ggplot2 scale for the \code{fill} aesthetic using sovpal palettes. Uses the
named palette exactly as defined.

For \code{constructivist}, the cream stop (#E8DFC8) is low-contrast even as a bar
fill; use the \code{"constructivist_core"} palette to exclude it.

Requires the \pkg{ggplot2} package.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)

  # Full GOST pipeline palette as bar fill
  ggplot(mtcars, aes(factor(cyl), fill = factor(cyl))) +
    geom_bar() +
    scale_fill_sovpal("gost14202")

  # Colorblind-safe diverging heatmap
  ggplot(faithfuld, aes(waiting, eruptions, fill = density)) +
    geom_tile() +
    scale_fill_sovpal("hazard_cvd", discrete = FALSE)

  # Constructivist without the low-contrast cream ground
  ggplot(iris, aes(Species, Sepal.Length, fill = Species)) +
    geom_boxplot() +
    scale_fill_sovpal("constructivist_core")
}

}
