% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2-scales.R
\name{scale_color_sovpal}
\alias{scale_color_sovpal}
\alias{scale_colour_sovpal}
\title{ggplot2 color scale for sovpal palettes}
\usage{
scale_color_sovpal(palette = "gost14202", discrete = TRUE, direction = 1, ...)

scale_colour_sovpal(...)
}
\arguments{
\item{palette}{Character. Palette name. Default \code{"gost14202"}.}

\item{discrete}{Logical. If \code{TRUE} (default), uses a discrete scale. If
\code{FALSE}, uses a continuous gradient via \code{\link[ggplot2:scale_gradient]{ggplot2::scale_color_gradientn()}}.}

\item{direction}{Integer. \code{1} (default, normal) or \code{-1} (reversed).}

\item{...}{Additional arguments passed to the underlying ggplot2 scale.}
}
\value{
A ggplot2 scale object.
}
\description{
A ggplot2 scale for the \code{colour} aesthetic using sovpal palettes.

The scale uses the named palette exactly as defined -- no colors are dropped
or interpolated for a discrete scale. For points and lines on a white
background, request a visualization-optimized palette by name (e.g.
\code{"gost14202_lines"}) rather than relying on hidden behavior.

Requires the \pkg{ggplot2} package.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)

  # White-background-safe lines palette
  ggplot(iris, aes(Sepal.Length, Sepal.Width, colour = Species)) +
    geom_point() +
    scale_color_sovpal("gost14202_lines")

  # Full GOST pipeline palette
  ggplot(iris, aes(Sepal.Length, Sepal.Width, colour = Species)) +
    geom_point() +
    scale_color_sovpal("gost14202")

  # Continuous steppe gradient
  ggplot(faithfuld, aes(waiting, eruptions, colour = density)) +
    geom_point() +
    scale_color_sovpal("steppe", discrete = FALSE)
}

}
